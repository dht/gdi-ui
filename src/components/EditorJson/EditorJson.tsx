import { Wrapper } from './EditorJson.style';

export type EditorJsonProps = {};

export function EditorJson(_props: EditorJsonProps) {
  return (
    <Wrapper className='EditorJson-wrapper' data-testid='EditorJson-wrapper'>
      EditorJson
    </Wrapper>
  );
}

export default EditorJson;
