import styled from 'styled-components';

export const Wrapper = styled.button`
  &.link {
    background: none;
    border: none;
    color: #1a1a1a;
    font-size: 16rem;
    margin: 0;
    cursor: pointer;
    text-decoration: underline;
  }
`;
